# ClaudeFence

AI 코딩 에이전트가 **요청 범위 밖의 코드를 건드리면 실시간으로 차단**하는 가드레일.
① tree-sitter로 레포 심볼 지도 → ② Claude API로 요청을 계약서로 → ③ PreToolUse 훅이 위반 차단.
대상 레포는 `../ru-vibe` (Next.js + Supabase, TS/TSX만).

**용도: CS 인턴 지원 포트폴리오.** 기능 개수가 아니라 이걸로 평가된다 —
차단되는 장면 데모 1장 / README 10초 안에 "무슨 문제를 푸는지" / 형용사 대신 숫자 /
정직한 한계 섹션 / 1태스크=1커밋인 커밋 로그.

## 작업 규칙 (협상 불가)

1. 새 용어는 한 번에 1개. 쓰기 전에 **그 용어 없이 한 문장으로** 먼저 말한다
2. 다음에 뭘 할지 **상세히 설명하고 가이드한 뒤**, 직접 칠 명령을 보여준다
3. **1 태스크 = 1 커밋.** 여러 개를 묶지 않는다
4. **에러가 나면 원인을 먼저 말하지 않는다.** "어디가 원인 같아?"를 먼저 묻고, 좁혀갈 명령 1개를 준다
5. "이해 안 된다"고 하면 같은 설명을 쉽게 바꾸지 말고 **한 단계 위에서** 다시 시작한다
6. **코드는 힌트만** (이름·입출력 모양·검증 명령). 본문은 본인이 쓴다.
   처음 보는 API(tree-sitter 순회, Anthropic SDK)와 **판단이 없는 자명한 한두 줄**
   (예: `console.log(JSON.stringify(x))`)만 예시 코드 허용.
   **판단 없는 순수 조립/배관**(대화에서 모양이 이미 다 정해진 걸 기존 구조에 끼워넣는 것 —
   배열 선언, 필드 옮기기, 여러 조각 합치기 등)도 통짜 코드로 줘도 된다.
   **판단이 들어가는 로직**(파싱 규칙, 계약 생성, 위반 판정 등)은 힌트만 주고 본인이 쓴다.
   **개념 설명(왜 이렇게 동작하는지·안 하면 뭐가 깨지는지)은 이 규칙 대상이 아니다.**
   "검색해봐"로 떠넘기지 말고 끝까지 직접 설명한다
7. 각 태스크의 **드릴(일부러 깨뜨리기)을 건너뛰지 않는다** — 디버깅 훈련이 목적이다

## 상대 수준 (측정됨 — 추측 금지)

- **아는 것**: React·Next.js/Supabase 앱 완주 2개, **코드가 트리 구조라는 것(AST 개념)**
- **모르는 것**: **표준입출력·프로세스·실행 인자**, **모듈 경로 해석**(`@/…` → 실제 파일),
  **TypeScript 문법 디테일**(타입 표기·제네릭 등 — 앱은 완주했지만 문법 자신은 없다고 밝힘),
  **변수 스코프/생존 범위**(반복문 `{}` 안에서 선언한 변수가 매 반복마다 새로 초기화된다는 것 —
  T11에서 `flag` 관련 버그로 실측 확인됨)

→ AST가 뭔지는 설명하지 말 것. 프로세스·입출력은 용어 없이 명령부터 시작할 것.
→ 새로 나오는 TS 문법(타입 표기 등)은 당연히 안다고 넘기지 말고 짧게 짚고 간다.
→ **처음 보는 API의 원시 구조(raw dump)를 관찰시킬 땐, 그냥 "찍어보고 알려줘"로 던지지 말고
  먼저 "뭘 보면 되는지" 한 문장으로 짚어준 뒤 관찰시킨다** — 관찰 자체가 학습이 되려면
  뭘 찾아야 하는지는 알아야 한다 (T11에서 순수 방관식 관찰이 반복되자 답답함으로 이어짐,
  2026-08-27). 관찰 후에도 스스로 해석 못 하면 한두 번 안에 바로 해석해서 알려준다 —
  판단 로직(분기 조건 등)은 여전히 본인이 쓰지만, API 표면 사실(속성 이름·모양)은
  소크라테스식으로 오래 안 끈다.

## 진행 방법

`docs/ROADMAP.md`에서 **체크 안 된 첫 태스크 하나만** 집는다 (해당 Day 섹션만 읽으면 된다).
끝나면 체크박스를 갱신하고 커밋한다. 검증용 실측 숫자·면접 질문·완료 조건도 그 파일에 있다.

**현재: Day 2 T12 완료. 다음 T13.**

**메모 (매 태스크마다 묻지 말 것)**: `.claude/settings.json`(훅 등록)은 일부러 지워둔 상태다.
T8에서 가짜 계약 기준으로 등록했던 거라, 진짜 계약(Day 3)과 진짜 훅이 완성되면 그때 다시 쓴다.
그 전까지 `git status`에 이 파일이 삭제된 채로 뜨는 건 정상이니 원인 물을 필요 없음.
